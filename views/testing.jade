extends layout

block content
	h1= title
	p Welcome #{locals.user.FirstName}!

	form(name='secretForm', method='post', action='/testing')
		label What's the secret password?
			br
			input(type='text', name='secretPassword', id='secretz')
		label Now, enter the secret code!
			br
			input(type='text', name='secretCode', id='codez')
		input(type='submit', value='Can you enter?', id='enter')